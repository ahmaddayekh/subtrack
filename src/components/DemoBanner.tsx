export function DemoBanner() {
  return (
    <div className="btn-gradient px-4 py-1.5 text-center text-xs font-bold text-white sm:text-sm">
      🚧 Portfolio demo — test data only, payments run in Stripe test mode, no real charges
    </div>
  );
}
